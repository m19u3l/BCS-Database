// email.js (Root Directory Placeholder)

import express from 'express';
const router = express.Router();

// Define a placeholder route or leave empty
router.get('/', (req, res) => {
    res.status(200).json({ message: "Email module is a placeholder." });
});

export default router;
